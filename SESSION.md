# Session journal

- 2026-08-26 19:22 — dispatched to claude: Build the entire Language Globe project in the current directory (an already-initialized git repo). 
- 2026-08-26 19:52 — dispatched to codex: This project (Language Globe) was mostly built by a previous AI session that was cut off by a usage 
- 2026-08-26 19:56 — Fixed frontend typecheck failures by wiring GlobeView's zoom handler into react-globe.gl, using Recharts' TooltipContentProps correctly, and splitting Vite/Vitest config so their Vite type versions do not collide. Added README.md, expanded .gitignore for SQLite sidecar files, verified typecheck/build/tests from root. Feature audit found the PLAN.md core pieces implemented: day/night shader, station pins/rings, surprise-me, hls.js player, quiz capture/transcribe/generation/music threshold, quizEnabled health flag, SQLite stats, passport UI, and loading/error states. No unresolved blockers; live OpenAI quiz behavior still depends on a real API key and reachable radio streams.
- 2026-08-26 20:26 — dispatched to codex: Build the React Native mobile app for this project. The complete mobile spec is in PLAN-MOBILE.md — 
- 2026-08-27 — Built the Expo mobile app with native Expo GL/three-globe rendering, copied API/types/solar helpers, Expo Audio playback with background config, quiz and stats panels, mobile API base resolution, README mobile instructions, and HOST docs in server/.env.example. Server source already had HOST support and permissive CORS (`origin: true`). Verified `npx tsc --noEmit` in mobile, `npx expo export --platform ios`, and `npm test --workspace server`; export only warned about three package example-loader export entries.
- 2026-08-26 20:41 — dispatched to codex: Add the live-captions feature to this project. The complete spec is in PLAN-CAPTIONS.md — read it fi
- 2026-08-27 — Added live captions end to end. Server now exposes POST /api/captions using the existing captureStream/transcribe flow with CAPTION_CHUNK_SECONDS, captionsEnabled/captionChunkSeconds health fields, cleanup/error handling, and a tested music-marker threshold. Web and mobile now have CC toggles, abortable one-request-at-a-time caption loops, panels above the player, and quiz-open auto-pause/resume. Verified root typecheck/build/test and mobile `npx tsc --noEmit`; live behavior still depends on OPENAI_API_KEY and reachable station streams.
- 2026-08-26 20:54 — dispatched to codex: Implement local model support for this project. The complete spec is in PLAN-LOCAL-MODELS.md — read 
- 2026-08-27 — Implemented local model providers. Server now resolves transcription via local whisper.cpp (resident whisper-server with whisper-cli fallback) or OpenAI, and quiz generation via Ollama or OpenAI, with health reporting resolved providers and feature flags based on any available provider. Quiz and captions routes share the provider layer; web/mobile disabled copy now mentions local models plus OpenAI fallback, and mobile disables Quiz from health. Added provider selection and Ollama parsing tests. Verified root typecheck/build/test and mobile `npx tsc --noEmit`; no git commit made. Did not start the app server during verification, so no whisper-server was spawned.
- 2026-08-26 21:04 — dispatched to codex: Implement captions v2 for this project. The complete spec is in PLAN-CAPTIONS-V2.md — read it first,
- 2026-08-26 21:11 — dispatched to codex: A previous session implementing captions v2 was cut off mid-run by a network disconnect, leaving unc
- 2026-08-26 21:11 — dispatched to cursor: A previous session implementing captions v2 was cut off mid-run, leaving uncommitted partial work. T
- 2026-08-26 21:16 — Finished captions v2. Prior session had already built the persistent CaptionSession + CaptionSessionStore (POST /api/captions/session, long-poll GET after=seq, DELETE, 10-min expiry, 2-session cap), an audio-relay GET /api/captions/session/:id/audio?delay=<s>, the burst-window discard, refactored web CaptionsPanel with Sync/Live toggle wired through radio.setAudioUrlOverride, mobile session polling, and a captionSessions.test.ts covering seq assignment, after= filtering, expiry and the concurrent cap; the old stateless POST /api/captions was already removed. Fixes I made: (1) mobile CaptionsPanel effect failed strict-null tsc because station could be null inside the async loop — captured stationId in a local before the async function; (2) burst-window bytes were still being pushed into the audio relay buffer, so Sync mode would have replayed the 30s-old icecast burst while captions came from post-burst audio — skip pushAudio while droppingBurstWindow is true so the relay starts near-live too; (3) sweepTmpDir only cleaned capture-* files, so a crashed caption session leaked caption-*.mp3 temp files across restarts — sweep both prefixes at boot. Verified npm run typecheck, npm test (41 pass incl. 4 new captionSessions tests), npm run build, and `npx tsc --noEmit` in mobile/. Sync-mode audio for mobile is intentionally deferred per the spec; PLAN-CAPTIONS-V3.md (word-level karaoke highlighting) is untracked and not in scope for this task. No git commit; no long-running processes started.
- 2026-08-26 21:21 — dispatched to gemini: Implement captions v3: word-level karaoke highlighting in Sync mode, per the spec in PLAN-CAPTIONS-V
- 2026-08-26 21:27 — dispatched to cursor: Implement captions v3: word-level karaoke highlighting in Sync mode, per the spec in PLAN-CAPTIONS-V
- 2026-08-26 21:40 — Shipped captions v3. Server: new `server/src/lib/whisperWords.ts` merges whisper-cli tokens into words and normalises whichever "flat" shape the provider hands back (OpenAI verbose_json, newer/older whisper.cpp server, raw offsets). `services/openai.ts` now has `transcribeVerbose` that requests `response_format=verbose_json` + `timestamp_granularities=['word']` on whisper-1; `services/providers.ts` exports a `transcribeChunk(): Promise<TranscribeResult>` that routes local through whisper-server (verbose_json) with a whisper-cli `-ojf` fallback and OpenAI otherwise, with `transcribeAudio` kept as a thin `text`-only wrapper so the quiz path is unchanged. `CaptionSession` now anchors a session timeline on the first surviving byte after the burst drop, freezes `startMs`/`endMs` per chunk at close time, and pushes per-word timings onto the session axis (chunkStart + wordRelative). Wire types (`CaptionChunk` + new `CaptionWord`) mirrored to frontend/mobile. Web: `useRadio` exposes `getAudioElement`; new `frontend/src/lib/captionSync.ts` maps `audio.currentTime` → session ms with a re-anchoring helper (uses wall clock at each new chunk to snap out MP3 drift) plus binary-searched `findActiveWordIndex`/`findActiveChunk`; `CaptionsPanel` runs a rAF loop in Sync mode and renders the current chunk word-by-word with past/current/future spans (accent + glow on current). Live mode still renders plain chunks. Mobile: types-only bump; delayed-audio karaoke on mobile is out of scope per spec. Key decisions: (1) sessionStartMs = first non-burst pushAudio time, so `audio.currentTime=0` on the relay ≈ session-offset 0 and per-frame math is just `currentTime*1000 + baseOffset`; (2) transcribeChunk is a single function used by both quiz (via wrapper) and captions to avoid two divergent paths; (3) word timings are always reported on the SESSION axis (already offset by chunkStart) so the client compares one number, no arithmetic per frame; (4) re-anchor uses wall clock (`performance.now()` diff from session-create) rather than chunk metadata, so a slow transcription does not desync the highlight. Verified `npm run typecheck`, `npm test` (50 pass server incl. 7 new whisperWords tests, 21 pass frontend incl. 12 new captionSync tests), `npm run build`, and `npx tsc --noEmit` in mobile — all clean. Not verified: real whisper-server verbose_json shape (there's no OPENAI key or whisper.cpp binary present here); the extractor is defensive across three known shapes and falls through to text-only if none match. No git commit; no long-running processes started.
- 2026-08-26 21:43 — dispatched to cursor: Implement the favorites feature per the spec in PLAN-FAVORITES.md in this repo (read it fully, plus 
- 2026-08-26 21:53 — Shipped favorites. Server: new `server/src/lib/favorites.ts` owns the SQLite `favorites` table (UNIQUE(user_id, station_id), snapshot fields per the spec) and exposes `createFavoritesStore(db)` with `list/add/remove/has`. `db.ts` instantiates a singleton `favoritesStore` on the shared DB; the store creates its own table so an in-memory `Database(':memory:')` can be used in tests. Routes in `server/src/routes/favorites.ts`: GET `/api/favorites` (joins each record against the live station index and marks unknown rows `missing`), PUT `/api/favorites/:stationId` (404s when the station is not in the current index; upserts so re-favoriting refreshes the snapshot but keeps `created_at`, which also makes the operation idempotent), DELETE `/api/favorites/:stationId` (204). Wire types `Favorite` / `FavoritesResponse` mirrored to frontend and mobile. Tests: `test/favorites.test.ts` covers empty/has, add+list, idempotent re-add (keeps created_at, refreshes name/country), remove w/ return value, newest-first ordering, and user_id scoping — 6 new tests, 56 total server. Web: `useFavorites` hook owns the list + a Set of ids for cheap membership; toggle is optimistic (revert on error). PlayerBar has a heart button next to the station name that flips filled/outline; App wires a "Favorites" HUD button (with a small count badge) next to Progress that opens `FavoritesPanel` (glass modal, flag + name + place + local time + kind chip + "offline in the index" hint when `missing`, click-to-tune closes the panel, trash-heart removes; Esc closes). `GlobeView` accepts `favoriteIds` and paints favorites the warm gold `FAVORITE_COLOR` (`#ffcf6a`) with a 1.25× radius bump so a rarely-clicked favorite still stands out. Mobile parity: identical hook, `FavoritesPanel` `Modal`, a heart button in the player bar, a `♥ N` HUD action, and gold favorites on the native globe. Key decisions: (1) one canonical wire shape (`Favorite = { createdAt, missing, station }`) — `station` is either the live one or reconstructed from the snapshot, so the client never has to switch code paths; (2) `PUT` upserts (idempotent) but keeps the original `created_at` so the list order stays put when the user re-favorites a renamed station; (3) missing favorites still carry their original URL so tune still works, but their kind color yields to gold + a `missing` badge; (4) the store module owns its table + statements so tests can use `:memory:` without booting the whole DB layer, matching how `captionSessions` is testable. Verified `npm run typecheck`, `npm test` (server 56 pass incl. 6 new; frontend 21 pass), `npm run build` from root, and `npx tsc --noEmit` in `mobile/` — all clean. No git commit; no long-running processes started. Not verified live: PUT against a real Radio Browser lookup (the station index is fetched at request time). Worth watching: `getStations()` inside GET/PUT can return `stale` when the upstream is down, which is fine, but a full cold failure on PUT surfaces as `stations_unavailable` (502) rather than falling back to the snapshot — that's intentional so we don't silently create favorites for phantom stationIds.
- 2026-08-26 23:05 — Fixed karaoke misalignment for real, verified by measurement (345cb5a). Diagnosis was done with an instrumented Playwright run (`/tmp/lg-e2e/watch3.mjs`) that intercepts caption poll responses and, every second, compares the DOM-highlighted word against the word the relay audio clock says should be lit. Before: 27% match, highlight ~8s ahead. Two root causes. (1) whisper.cpp word timings were fake — `t_dtw=-1`, times were linear interpolation across each segment ("Suscríbete" allegedly 3.8s). Fix: spawn whisper-server with `--dtw large.v3.turbo --no-flash-attn` (flash-attn silently disables DTW; config `whisperDtwPreset`, env `WHISPER_DTW_PRESET`), and `normalizeFlatWords` now prefers `t_dtw` (centiseconds, ≈token end; token spans [prev end, own t_dtw]) when ≥80% of entries carry it. Costs ~1.5s per 15s clip — acceptable. (2) The audio relay consumed its byte buffer destructively, so any second browser connection (preload probe, StrictMode double-mount) started mid-stream, breaking the currentTime↔session mapping. Relays now keep per-connection cursors (chunk `seq`) over the shared append-only buffer and always serve from the start; buffer is trimmed by age only (180s cap). With that guarantee, `audio.currentTime` IS the session clock — deleted `reanchorPlayback` and the anchor machinery entirely from `captionSync.ts`/`CaptionsPanel.tsx`; the wall-clock snap was the thing pushing the highlight ~8s (the buffering margin) ahead. After: 97% word-for-word match, median offset 0ms, p90 0ms, on a station playing song lyrics. Note: reconnects also resolve correctly now (relay replays from session 0), but a reconnect after the 180s buffer cap would misalign — accepted for v1. Verified: server 63 tests pass (3 new DTW tests, 1 new multi-connection relay test), frontend 19 pass, tsc clean both, live browser measurement above. Killed stale whisper-server processes so the app respawned with DTW flags.
- 2026-08-26 23:35 — Dockerized the app + GHCR publish on merge to main. New multi-stage `Dockerfile` (build: node:22-slim + python3/make/g++ because better-sqlite3 compiles from source on slim; runtime: node:22-slim + ffmpeg only, runs as `node`, `DB_PATH=/data/...` with a VOLUME). The Fastify server now serves the built frontend when `../frontend/dist` exists (new `@fastify/static` dep; SPA fallback for non-API GETs; no-op in dev where Vite serves). Frontend already used relative `/api` paths so one container serves UI+API on 8787. `.github/workflows/docker.yml` builds linux/amd64+arm64 with buildx/QEMU and pushes `ghcr.io/luke-mcevoy/language-globe:latest` + `sha-*` tags on push to main (GITHUB_TOKEN, packages:write, GHA cache). `.dockerignore` excludes mobile except its package.json (root workspace manifest needs it to parse; only server+frontend are installed). Local whisper/ollama NOT bundled (model ~1.5GB): captions/quiz need OPENAI_API_KEY (or OLLAMA_URL to a reachable ollama for quizzes). Verified locally: image builds, container boots, /api/health ok with ffmpegAvailable true, / serves the UI, SPA fallback works, /api/stations returns live stations from inside the container. README gains a Docker section. Note for repo owner: first GHCR push creates a PRIVATE package — flip it to public once in GitHub → Packages → language-globe → settings for anonymous pulls. Work done in a git worktree on main so the running dev tree (captions-ui-polish) was untouched.
- 2026-08-31 21:04 — dispatched to codex: Implement Phase 1 ONLY (synced karaoke captions) of PLAN-MOBILE-PARITY.md in this repo — read that f
- 2026-08-31 21:08 — Implemented Phase 1 mobile synced karaoke captions only. Added the pure mobile caption sync helpers from web, added a mobile caption relay audio URL helper, and taught `useRadio` to swap between station audio and a captions relay override while exposing a timestamped playback clock. `CaptionsPanel` now buffers until `chunkSeconds + 5 + 8s` of relay audio is available, shows sync progress, switches to delayed audio, interpolates the session clock on a 150ms interval, collapses music markers, and highlights the active word with RN nested text. Verified `npx tsc --noEmit` in `mobile/` and root `npm test`; no dev servers/simulators were started. Watch item: real-device Expo Audio `currentTime` cadence may still need live tuning, but the interpolation is clamped as planned.
- 2026-08-31 21:10 — dispatched to cursor: Implement Phase 2 ONLY (tap a word to translate + vocab list) of PLAN-MOBILE-PARITY.md in this repo 
- 2026-08-31 21:16 — Implemented Phase 2 mobile parity: tap-a-word translate + vocab list. Mirrored the web `cleanWord` regex and `handleWordClick` flow in `mobile/src/components/CaptionsPanel.tsx`; every token is now tappable (nested `<Text onPress>`) in BOTH karaoke and plain chunks — plain chunks split on `/(\s+)/` so tokens survive the change without word-level timings. Tap → pause radio → open lookup sheet → `lookupWord(cleanedWord, chunk.text, station.name)`. The sheet is docked inside the panel above the footer (RN can't cheaply anchor to a nested Text run, so a fixed bottom sheet per the plan): accent-bold word, `translating…`/error/translation+note+"✓ Saved to your vocab · looked up N×", and two buttons — ▶ Resume (accent, closes+resumes) and Close (closes, stays paused). Bumped panel `maxHeight` 300→380 to fit the sheet + footer without clipping the caption feed. Added `pause()`/`resume()` to `useRadio` mirroring the web hook (explicit, non-toggling; `resume` no-ops if already playing so it never spuriously kicks a paused stream); wired them via new `onPauseAudio`/`onResumeAudio` props from `App.tsx`. Added `lookupWord`/`getVocab`/`removeVocabWord` to `mobile/src/lib/api.ts` and `VocabEntry`/`VocabResponse`/`VocabLookupResponse` to `mobile/src/types.ts` — identical shapes to `frontend/`. `StatsPanel` gained a `VocabSection` that fetches on mount, renders nothing when empty (so first-time users don't see a blank section), and shows each row as `word — translation` with an optional dimmed note beneath, an `N×` badge when `timesLookedUp > 1`, and a ✕ that optimistically removes then calls `removeVocabWord`. The section appears at the bottom of the passport body AND in the "no quizzes yet" empty state (wrapped the empty state in a `ScrollView` so the vocab list still scrolls even when quiz history is empty, matching web). Verified `npx tsc --noEmit` in `mobile/` (clean) and `npm test` from repo root (server 80/80, frontend 10/10). Touched only `mobile/`; no git commit; no dev servers/simulators started. Watch item: on Android the lookup sheet's translucent green tint (`rgba(5,32,25,0.55)`) sits over the panel's `rgba(9,14,29,0.92)` background, which on some devices may render darker than intended — safe to tune if the sheet reads as too muted in practice.

- 2026-08-31 21:16 — dispatched to cursor: Implement Phase 3 ONLY (ambient scene card) of PLAN-MOBILE-PARITY.md in this repo — read that file f
- 2026-08-31 21:22 — Implemented Phase 3 mobile ambient scene card. Copied `SceneResponse` from `frontend/src/types.ts` into `mobile/src/types.ts` (unchanged shape: `image` data-URL, `prompt`, `basedOn`, `seconds`). Added `generateScene(sessionId, signal?)` to `mobile/src/lib/api.ts` mirroring `frontend/src/api.ts` (POST `/api/scene` with `{ sessionId }`, optional AbortSignal). `mobile/src/components/CaptionsPanel.tsx` gained a `scenesEnabled: boolean` prop wired from `App.tsx` via `health?.scenesEnabled ?? false`; the panel now hosts a scene loop that mirrors the web `useEffect`: on `sessionId` present + `scenesEnabled`, fire immediately, then a 5s tick; each tick early-returns unless `SCENE_INTERVAL_MS = 45_000` has elapsed since the last successful draw AND a scene already exists (first tick still fires with no delay, matching web). One request in flight (`sceneInflightRef`); failures fall through and next tick retries. The mobile-only wrinkle vs. web: each request runs under a fresh `AbortController` with a `SCENE_FETCH_TIMEOUT_MS = 90_000` timeout so a hung sidecar can't stall the loop forever (RN fetch has no default timeout guarantees, and the plan explicitly calls for a generous fetch timeout because first-gen can take ~45s). Cleanup aborts the in-flight controller + clears the interval. Cross-fade: kept the same `Scene` shape (`src`, `prompt`, `key: Date.now()`) and `prevScene` layering as web, but implemented the fade with `Animated.Value(0→1)` via `Animated.timing` for `SCENE_FADE_MS = 1_400` on `useNativeDriver: true`; the previous image sits at opacity 1 underneath a `<Animated.Image>` that fades in, and `setPrevScene(null)` fires only when the animation `finished` callback lands (so an interruption doesn't leave the old image on top of a half-faded new one). Card renders between the header and the sync row (matches the web position "at the top of the feed"), 3:2 `aspectRatio`, 10px rounded, `#060a15` bg, hairline border. RN has no CSS gradient primitive and `expo-linear-gradient` is not installed, so the prompt overlay uses a solid `rgba(4, 7, 14, 0.78)` bottom bar with `paddingTop: 18` to approximate the web `linear-gradient(to top, rgba(4,7,14,.88), transparent)`; text is 2-line clamped via `numberOfLines={2}`. Scene state (`scene`, `prevScene`, `sceneRef`, `lastSceneAtRef`) resets in the existing station-change effect so tuning a new station starts fresh — same as web. When `!scenesEnabled` or no `sessionId`, the effect returns early and nothing renders (no placeholder box) per spec. Verified `npx tsc --noEmit` in `mobile/` (clean) and `npm test` from repo root (server 80/80 across 10 files, frontend 10/10). Touched only `mobile/` (`src/types.ts`, `src/lib/api.ts`, `src/components/CaptionsPanel.tsx`, `App.tsx`). No git commit; no dev servers/simulators started. Watch item: no live end-to-end verification against a running sidecar was possible in this session — the interval-based tick was designed to be resilient to a cold/absent sidecar (silent retry) but a real device run should confirm the ~1.4s fade doesn't hitch when big base64 PNGs decode.
- 2026-08-31 21:39 — dispatched to cursor (grok-4.6): deploy — merge captions-ui-polish to main, GHCR publish, run container locally
- 2026-08-31 21:45 — Deployed. Pruned stale `/tmp/lg-docker` worktree, then tried `git fetch . captions-ui-polish:main` (local FF 002a820→b398a48) + `git push origin main`. Origin rejected: `origin/main` had merge commit `d66e879` (PR #2) that is not an ancestor of captions-ui-polish. Created merge commit `5998b78` on local main via `commit-tree` (no checkout; working tree stayed on captions-ui-polish) and pushed `d66e879..5998b78`. GHCR workflow run 33459678633 (docker, push, head `5998b78`) went queued → in_progress → completed/success in ~4 min (https://github.com/luke-mcevoy/language-globe/actions/runs/33459678633). Local: `docker build -t language-globe:local .` then `docker run -d --name language-globe -p 8890:8787 -v lg-data:/data -e OLLAMA_URL=http://host.docker.internal:11434` — container `d553dc114aa3` left RUNNING. Health: `{"ok":true,"quizEnabled":true,"captionsEnabled":false,"targetLanguage":"spanish","captureSeconds":60,"captionChunkSeconds":15,"transcribeProvider":"unavailable","quizProvider":"ollama","ffmpegAvailable":true,"scenesEnabled":false}`. `/` serves the app HTML. Note: live captions/transcription inside the container need `OPENAI_API_KEY` (whisper.cpp is not bundled); quizzes work via host Ollama through `OLLAMA_URL`. No source files changed; no force-push; no dev processes killed.
- 2026-08-31 21:46 — dispatched to cursor (grok-4.6): remove ambient scenes + enforce local-models-only per PLAN-LOCAL-ONLY.md
- 2026-08-31 21:52 — Implemented PLAN-LOCAL-ONLY.md on captions-ui-polish. Phase A: deleted `server/src/routes/scene.ts`, `services/scenes.ts`, `test/scenes.test.ts`, and the whole `scene-server/` sidecar; stripped `scenesEnabled` / `SceneResponse` / `generateScene` / scene UI from server, web, and mobile; dropped `recentText()` from CaptionSession; removed scene CSS and the `.dockerignore` sidecar line. Phase B: moved provider-neutral prompt builders/types into `server/src/lib/prompts.ts` and deleted `services/openai.ts` plus the `openai` npm dep. Providers now resolve only `local-whisper` / `ollama` (unknown modes → auto); local failure rethrows with no cloud fallback. Health unions, `.env.example`, README, PLAN.md, and disabled UI copy now name the missing local model (whisper.cpp `WHISPER_MODEL_PATH` / `ollama pull qwen2.5:7b-instruct`). Cleared the leftover API key from gitignored `server/.env`. Verified `npm run typecheck`, `npm test` (server 73, frontend 10), `npx tsc --noEmit` in `mobile/`, and `rg -i openai --glob '!SESSION.md' --glob '!PLAN-*.md'` empty. No git commit; did not touch running dev servers or the language-globe Docker container.
- 2026-08-31 22:02 — dispatched to cursor (grok-4.6): cinematic center-stage karaoke subtitles for web captions
- 2026-08-31 22:04 — dispatched to cursor (grok-4.6): Phase 1 of PLAN-SOCIAL.md — accounts, sessions, per-user data (server only)
- 2026-08-31 22:10 — Implemented PLAN-SOCIAL.md Phase 1 (server accounts + per-user data). New `server/src/lib/auth.ts` owns `users` (uuid PK, username UNIQUE COLLATE NOCASE, scrypt `salt:hex` with N=16384, constant-time verify) and `sessions` (sha256 of a 32-byte token, 30-day expiry). `createAuthStore(db)` mirrors the favorites store. Existing INTEGER `users(id, name)` tables are renamed to `users_legacy` so old quiz/vocab/favorites rows (user_id = 1) stay put and new accounts start empty — not a data migration. `@fastify/cookie` session cookie `lg_session` (httpOnly, SameSite=Lax, path=/, 30d, secure: 'auto'). Routes: POST `/api/auth/signup` 201, POST `/api/auth/login` 200 + 10/15min/IP Map rate limit, POST `/api/auth/logout` 204, GET `/api/auth/me` `{ user }` or null. `resolveUser` / `requireUser` send 401 `{ error: 'account_required' }`. Quiz start/submit/transcript, stats, vocab, and favorites now use the session user; health/stations/captions/`/api/quiz/config` stay public. Wire types `AuthUser` / `MeResponse` mirrored to frontend + mobile types files only. Tests: auth store (signup/login/wrong password/expiry/legacy table) and route signup→me→logout plus anonymous 401 on vocab/favorites. Updated favorites/vocab store tests to string user ids. Verified `npm run typecheck` and `npm test` (server 84, frontend 10) — clean. Did not open CaptionsPanel/styles.css/App.tsx. No git commit; no processes left running.
